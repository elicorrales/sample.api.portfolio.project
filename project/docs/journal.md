# Project Journal

How this project came together, step by step. Each step links to the detailed record.

Written in my voice: **I** means me, the project author, and **the AI** means Claude in Claude Code. See the [decision log key](decisions/README.md#whos-who).

## 2026-09-12 — From idea to spec

| # | Step | What happened | Record |
|---|---|---|---|
| 1 | Initial plan | Sketched the project with a different AI assistant: a Node/Express/PostgreSQL users API, built test-first. | [01](decisions/01-ai-cross-check.md) |
| 2 | Second opinion | Gave that whole chat to Claude to confirm or dispute. It caught an outdated claim about Node's test tooling, and several items the plan put off too long (validation, auth). | [01](decisions/01-ai-cross-check.md) |
| 3 | Stack and tests | Learned what each tool does. Defined my own test categories: happy path, bad calls, integrity, security, performance, rate limiting. | [02](decisions/02-stack.md), [03](decisions/03-test-strategy.md) |
| 4 | Scope | Chose an admin-only API first; self-service logins later. | [04](decisions/04-admin-vs-self-service.md) |
| 5 | Hosting | Planned local first, then Render (API + Swagger UI) and Netlify (pages), the services I already use. Identified the problem of letting demo visitors use an admin API. | [05](decisions/05-hosting.md) |
| 6 | Code layout | Had the AI compare the folder structure of my three earlier projects before proposing one for this API. | [06](decisions/06-layering.md) |
| 7 | User rules | Worked through fields, views, search, delete/restore, and locking in short question-and-answer rounds. **Key moment:** choosing optimistic locking removed PATCH's main advantage, so I chose PUT. | [07](decisions/07-users.md) |
| 8 | Phones and addresses | Set ownership, delete, and validation rules. Tightened several suggestions: one phone per type, 3 addresses max, 5-digit ZIP only. | [08](decisions/08-phones-addresses.md) |
| 9 | Record keeping | Decided repo docs are the record and AI memory holds only pointers. Backfilled this decision log from the conversation. | [09](decisions/09-docs-and-process.md) |
| 10 | Credit audit | Reviewed who each decision came from. The AI had credited me for several choices I had actually picked from its options, so I added a **picked** tag and retagged those decisions. | [Decision log key](decisions/README.md#whos-who) |
| 11 | Tooling fix | The Claude Code diff panel couldn't read git. Cause: the repo sits in a VirtualBox shared folder, where files appear owned by `root` and marked executable. Fixed with git's `safe.directory` and `core.fileMode false`. | — |
| 12 | Operations list | Turned the decisions into a plain-language list of all 16 operations, the step before the formal OpenAPI file. Filling in details surfaced five new questions; I accepted all five proposals. | [Operations](spec/operations.md) |
| 13 | HTTP conventions | The AI mapped the operations to paths, methods, and status codes, with 8 convention choices. Before accepting, I checked each choice from two angles: security and the future web client. That surfaced five extra rules (page size cap, sort allowlist, auth before existence, no leaked internals, CORS headers). **Pushback:** I challenged the AI's claim that the standard error format wasn't JSON. It is; the AI corrected its framing. | [10](decisions/10-api-conventions.md) |
| 14 | Docs page first | Before writing any endpoints, set up a Swagger UI page on the portfolio site that renders the spec, so the contract is visible (and hostable) from the first line. **Pushback:** the first version required copying the spec by hand before every local view. I rejected that, and the page now loads the spec from one path that works both locally and on Netlify. | [05](decisions/05-hosting.md#viewing-the-spec-before-the-api-exists) |
| 15 | Spec piece 1: shared parts | Wrote the reusable parts every endpoint will reference: admin token, error format, paging, IDs, version header, standard error responses. Ran a spec linter (Redocly) to check the file against OpenAPI rules; it flagged a missing server list and license, which led to adding `localhost:3000`, choosing MIT, and making linting a standing step. | [10](decisions/10-api-conventions.md#shared-components-spec-piece-1) |
| 16 | Spec piece 2: user endpoints | Wrote the 6 user operations into the spec. I questioned the proposed 100-character name limit ("what's realistic?"), which led to 50 per field, plus character rules that don't block real names like José, O'Brien, or García-Fernández de la Torre. The name pattern was tested against sample names before acceptance. | [07](decisions/07-users.md#field-details-spec-piece-2) |
| 17 | Spec piece 3: phones and addresses | Wrote the remaining 10 operations; the spec now has all 16 and passes the linter. Writing exact validation exposed a conflict between two earlier rules: "no duplicates" plus "one per type" would have blocked the common case of home address = mailing address. The duplicate rule was dropped. | [08](decisions/08-phones-addresses.md#details-and-a-reversal-spec-piece-3) |
| 18 | No formal spec review | I pushed back on a full spec review: it's greenfield, writing tests is itself a review, and the admin web client will prove the spec in real use. The spec stays at 0.x until then, and becomes 1.0.0 once verified end to end. | [03](decisions/03-test-strategy.md#formal-spec-review-skipped) |
| 19 | Database without Docker | No Docker and little disk space. Briefly considered MariaDB, but kept PostgreSQL (hosting support, earlier decisions). Chose a staged path: in-memory fakes, then PGlite (PostgreSQL inside Node), then a native PostgreSQL install. | [03](decisions/03-test-strategy.md#database-path-for-tests) |
| 20 | Test setup and first red test | Set up Vitest, Supertest, and TypeScript, plus an app skeleton that answers `501` to everything, and wrote the first happy-path test (create a user). `npm install` failed in the VM because the VirtualBox shared folder blocked symbolic links; fixed by enabling symlinks for that share on the host. I ran the test myself: it failed with `expected 501 to be 201`, which is the intended red: the test runs and fails for the right reason. | [02](decisions/02-stack.md#confirmed-at-test-setup) |
| 21 | One user form, one save | While planning the happy-path tests, I pictured how the admin page will actually be used: one form for create and edit, with all the user's info, and one Save. The spec didn't match: phones and addresses had their own endpoints, so a user could exist with none, or lose their primary. **Key moment:** thinking about real use, not just tests, reversed several earlier rules before any code was written. Phones and addresses are now saved as part of the user (at least one of each, exactly one primary, automatic when there's only one), with no ids of their own. **16 operations became 6.** Updated the decisions, operations list, spec (lint passes), and the first test, which I re-ran: still red for the right reason. | [08](decisions/08-phones-addresses.md#revision-one-user-form-one-save) |
| 22 | First vertical slice | After two red tests for create user, I noticed we had been going layer by layer (spec, then tests) and asked to go the other way: enough real code for one operation to work end to end, on a real server, called from the Swagger page. That's a **vertical slice** (also called a walking skeleton). Added a second test (3 phones and 3 addresses, sent out of order), which exposed a spec gap: "sorted by type" never said in which order (chose the listed order). Built create user through every layer: Zod validation, business rules, an in-memory repository, JWT auth, hand-written CORS, and Problem Details errors. The other 5 operations still answer `501`. Both tests went green on my run. Then I started the API, served the docs page, pasted a dev token (`npm run token`) into Swagger's Authorize, and created a user: `201`, then `409` on the same email. Restarting the API clears the data, as expected with in-memory storage. | [06](decisions/06-layering.md#as-built-first-vertical-slice-create-user) |
| 23 | Plain English characters | Planning the List tests raised questions about accents (does `garcia` find `García`? where does `Élise` sort?). I stepped back and decided that supporting every language was more than a sample project needs: names now allow only A–Z, spaces, hyphens, periods, and the straight apostrophe; street and city allow letters, digits, spaces, and `- , . #`. This reverses the "letters in any language" rule. While at it, settled two List questions: sorting ignores case, and a page past the end returns an empty page instead of an error. I asked how the web client would know when to show a page-nav (`<< < 1 2 3 > >>`); the existing response (`page`, `totalPages`, `totalItems`) already covers it, so the spec's list shape didn't change. | [07](decisions/07-users.md#revision-plain-english-letters-only), [10](decisions/10-api-conventions.md) |
| 24 | List users | Added a `createUser` test helper so each test sets up only the users it needs. Wrote 5 List tests (default order, search, sort by email descending, paging, page past the end); ran them red (`501`), then the code was built and all 7 tests went green. One small question came up: with `order=desc`, do tie-breakers reverse too? Kept them ascending (Smith Bob before Smith Zoe). Tried it several times from Swagger UI. | [07](decisions/07-users.md#listing) |
| 25 | Get one user | Wrote 2 tests (basic view by default; detailed view must equal what create returned), ran them red, then the code was built: 9 green. In Swagger my first try returned `404`: I had used the spec's example id, and the dev server's automatic restart had also wiped my users. **Caught a bug by reading the response headers:** Express was adding its own body-hash `ETag` to every response, including errors and lists, which would confuse clients that send the ETag back as the version. Turned that off; only responses that return a user carry an `ETag`. | — |
| 26 | Update user | Settled two rules first: saving with the user's own email isn't a conflict, and every save bumps the version, even with no changes. I asked what an ETag is actually for; the answer was the two-admins scenario, where the second save gets `412` instead of silently erasing the first. Wrote 2 tests (replace the whole user and confirm with a GET that it was saved; an edit leaving one phone makes it primary), ran them red, then the code was built: 11 green. Create and update now share the code that builds a user, and the version is re-checked at the moment of saving so two simultaneous saves can't both win. In Swagger: `If-Match: "1"` → `200`, same again → `412`. | [07](decisions/07-users.md#locking) |
| 27 | Delete user | Decided delete bumps the version, and split the checks into 3 tests (hidden from get and list; still visible with `includeDeleted`, with `deletedAt`; a deleted user can't be updated). Ran them red. One failed differently than the AI predicted (`412`, not `200`), because the test's `If-Match` assumed a delete that hadn't happened; still a valid red. Built delete as a mark (`deletedAt`), not a removal, so restore is possible: 14 green. In Swagger, a successful delete's `204` was easy to miss: it has no body, and Swagger's documentation section right below shows a sample `400`. Checked get with `includeDeleted` false (`404`), then true (`200`). | [07](decisions/07-users.md#delete) |
| 28 | Restore user: all 6 operations working | Agreed restore needs no `If-Match` (a deleted user can't be edited, so nothing can be overwritten). Wrote 2 tests with a user holding 2 phones and 2 addresses, so "intact" means something; both failed exactly as predicted this time. Built restore (`409` if the user isn't deleted): 16 green. Tried the whole cycle in Swagger. **Milestone:** every operation in the spec now works end to end on in-memory storage, each one built red → green → tried by hand. | [07](decisions/07-users.md#delete) |
| 29 | Workflow test: happy path complete | Added one test that follows an admin's session on a single user (create → list → get → edit → delete → restore), sending each step's `ETag` into the next save, the way the web page will. Unlike the others, it was written after the code, so it was expected to pass at once. It didn't: it expected version 3 after restore, but the API said 4. **The API was right:** the AI had copied the number from the Restore test, which has no edit step. The test caught the AI's own counting mistake. I asked whether to keep a deliberately wrong copy of the test to prove it can fail; decided no, since that would keep the suite permanently red. The real practice is **mutation testing** (e.g. Stryker), which breaks the code temporarily to check the tests notice. 17 green. | [03](decisions/03-test-strategy.md#test-categories) |
| 30 | Bad-call tests | Went through a list of ways a client can get it wrong (bad body, bad query, bad ids, versions, conflicts, paths) and settled 5 questions: unknown query parameters → `400`; unknown path → `404`, wrong method → `405` with `Allow`; order of checks; table-driven tests; a frozen clock for the "turns 18 today" boundary. Wrote 110 tests before touching code: **112 of 127 passed at once**, because most rules were built along with the operations; the 15 failures were exactly the predicted ones. One was a real gap: unknown fields were reported as `body` instead of by name. Fixed that (echoing only safe names, so `<script>` never comes back), added the new rules, plus 5 more tests: 132 green. **I pointed out these tests matter more than the happy path** (115 vs 17), and asked that the docs showcase them, and every test category after. | [10](decisions/10-api-conventions.md), [03](decisions/03-test-strategy.md#how-the-bad-call-tests-are-written) |
| 31 | Security tests | Went through 6 areas (tokens, auth first, injection, data leaks, CORS, body size) and settled 4 questions: auth before the body is read (**I asked that a test enforce it**), `413` for oversized bodies, `bearer` in any case (I asked why first: the HTTP standard says the scheme ignores case), and forcing a `500` with a fake repository that throws (I asked how that works). Wrote 60 tests, with forged tokens (`alg: none`, an edited role, no expiry) and look-alike origins. The AI predicted 11 failures; **49 passed and exactly those 11 failed.** Two were **real holes**: a signed token with no expiry was accepted forever, and every response said `X-Powered-By: Express`. Fixed both, moved auth ahead of body parsing, and added `413` to the spec: 192 green. In Swagger, the `401` didn't show `WWW-Authenticate`. **The AI's suggested check was wrong:** browser code can't see that header unless the API exposes it through CORS. `curl -i` showed it was there. I decided not to expose it, since a `401` is enough. | [04](decisions/04-admin-vs-self-service.md#auth-details-security-tests), [10](decisions/10-api-conventions.md), [03](decisions/03-test-strategy.md#how-the-security-tests-are-written) |
| 32 | Rate limiting | Chose rate limiting over the database as the next step. Agreed on 10 cases and 5 design choices: count per IP before auth (so token-guessing floods are stopped), a fixed one-minute window, 100 per minute, hand-written like CORS, and `X-Forwarded-For` ignored unless a proxy is trusted (so a client can't fake a new IP). Wrote 12 tests with a limit of 3 and a clock that only fakes `Date`, jumping from 12:00:00 to 12:00:30 to 12:01:00. Every test but one checks that a `429` really happens, so none could pass without a limiter. The AI predicted 11 failures; **exactly 11 failed** (one showed `201` rather than the predicted `200`, since it was a create). Built the limiter in about 30 lines, placed after CORS (preflights don't count, and a `429` still reaches the web page) and before auth: 204 green. Checked by hand with a `curl` loop: `100 401`, then `429` with `Retry-After: 17` at 04:18:43, exactly the seconds left in the window. | [10](decisions/10-api-conventions.md), [03](decisions/03-test-strategy.md#how-the-rate-limit-tests-are-written) |
| 33 | Database: PostgreSQL via Drizzle and PGlite | Before starting, I asked what publishing to Render would mean: in-memory data is lost on every restart, deploy, and free-tier sleep, and **PGlite alone doesn't fix that** on Render either; only a separate PostgreSQL server does. I reframed the step's value: real SQL tested, and a start on integrity. The AI's first pick was raw SQL, based on my "test actual SQL" comment; I said that was too loose and asked what employers expect and how Prisma hides SQL (I know Hibernate/JPA from Java). **The AI checked Prisma's current docs** and found concrete gaps for this project: no official PGlite adapter, no `lower(email)` index in the stable version, and partial indexes still in preview with an open bug. **I chose Drizzle.** Reviewed the first generated SQL migration before it ran. Accepted a dev-only `npm audit` warning after tracing it to an esbuild dev server `drizzle-kit` never starts. **The first full test run froze my laptop.** We measured rather than guessed: ~1.1 GB per test file's database × 7 in parallel, against 3.4 GB available with the VM running. The AI had predicted "slower," not a freeze. Offered 2 workers, **I chose 1** (2 only bought speed): about 50 s. Then **203 of 204 passed**: the workflow test caught phones returned in the wrong order, a rule in-memory storage had kept only by accident. Fixed with one shared sorting function: 204 green, no test assertions changed. Removed the in-memory repository; `npm run dev` now saves to `.data/`, and a user I created in Swagger survived a restart. | [11](decisions/11-database.md), [02](decisions/02-stack.md), [06](decisions/06-layering.md#as-built-database-2026-09-13) |
| 34 | Integrity tests | Went through 14 cases in 4 groups (two requests at once, one user never touching another, a failed save changing nothing, the database's own rules) and agreed on 4 choices. PGlite runs one query at a time, but the API checks, then saves, in separate steps, so a race is still possible. Instead of hoping two requests overlap, a **test-only repository holds both right after their check** and releases them together, so the race happens every run. A **temporary PostgreSQL trigger** makes a save fail midway, proving the transaction undoes it. Raw SQL tests of the database's own rules were allowed as an exception to "through the API only." **The AI predicted 3 failures; exactly those 3 failed**, including two it was unsure would even run (the trigger's language, and where Drizzle keeps the error code). **Real bug:** two requests with the same email at once got **`500` instead of `409`**. The database's unique index had refused the duplicate, so no bad data was saved, but the client got "Something went wrong." Fixed by recognizing that refusal (a named `EmailTakenError`) and answering `409`: 218 green. **Found along the way:** the failing run's server log showed the query **with names, emails, and dates of birth**. Callers never see it, but hosted logs shouldn't hold personal data. **I asked that it be recorded and showcased** along with the failures; it's an open item to decide before hosting. | [11](decisions/11-database.md#integrity-tests-2026-09-13), [03](decisions/03-test-strategy.md#how-the-integrity-tests-are-written) |
| 35 | Real PostgreSQL server | I asked two things first: does the VM need PostgreSQL (no, only the laptop runs tests and servers), and what's the smallest way to install it. Instead of a system install with sudo, **`embedded-postgres`** runs real PostgreSQL 18 server binaries from `node_modules`, which keeps my rule of everything local to the repo. **I asked whether that carries over to hosting:** everything but `embedded-postgres` itself does, because the API only reads `DATABASE_URL`, and on Render the host runs PostgreSQL. Removed PGlite. Tests now start one server per run and give each test file its own database, copied from a migrated template. Before running anything, the AI read how `embedded-postgres` shuts down on Ctrl+C and flagged a rare ~10 s wait; it didn't happen. **218 green on the first run, no test changed, 65 s → 22 s**, and the race tests now use truly simultaneous connections. In Swagger, a user survived a stop and restart of `npm run dev`. | [11](decisions/11-database.md#stage-3-a-real-postgresql-server-2026-09-13), [06](decisions/06-layering.md#as-built-real-postgresql-server-2026-09-13) |
| 36 | Before hosting: docs and encryption | Settled the plan: host the API on Render, then build the admin web client, and **skip performance tests** (to be noted as a next step, since I need to stop the project after that). **I decided not to host the docs separately:** the journal, decisions, and test showcase are already readable on GitHub, and the API will serve its own Swagger page at `/docs`. **I raised encryption,** which we had never discussed. The AI laid out three layers (in transit, provider disk encryption, and field-level) and what field-level encryption would break here: search, sorting, and the unique email rule, plus where the key would live. **I chose to write it up thoroughly and build none of it.** The write-up covers what doing it properly would take. | [12](decisions/12-encryption.md), [05](decisions/05-hosting.md#revised-at-hosting-time-2026-09-13) |
| 37 | Hosting plan: 8 questions | Walked through every hosting question before writing code, one decision at a time. **Database:** on free plans the AI picked Neon, since Render's free database is deleted after 30 days; my games already run on paid Render instances, and **once I said paid was fine the pick changed** to Render Postgres ($6) plus a Starter web service ($7) that never sleeps: PostgreSQL 18 like the tests, reached over Render's private network with SSL, with outside access off. **I asked why migrations matter** if the project won't change; the empty hosted database needs its tables once, and the API already applies migrations when it starts (the AI had said it needed planning before checking). **Demo token:** `POST /demo/token`, only in demo mode; **I pointed out** visitors who skip it can watch every call get `401`. **Logs:** only safe error fields, with a test; **I asked how real products keep logs useful without personal data** (log ids and request ids, not values), documented but not built. `TRUST_PROXY` gets measured after deploy, not guessed. The API will run as plain `node` on its TypeScript. **I added two ideas:** 50 starting users so the web client has pages, and a 200-user cap. **Two corrections from me:** Netlify is for the admin web client, not dropped (Swagger isn't a website for non-technical visitors), and **`website/` was always meant for that client**, a misreading the AI had carried since day one. **The reset took three rounds:** I turned down a daily wipe, preferred emptying the oldest deleted users, rejected the follow-up because it still needed me, and came back to the wipe with a reason: the demo must never need me, so reset nightly at 08:00 UTC, the middle of the night across the US. | [05](decisions/05-hosting.md#swagger-served-by-the-api-netlify-kept-for-the-web-client-2026-09-13), [11](decisions/11-database.md#found-along-the-way-personal-data-in-server-logs-open), [10](decisions/10-api-conventions.md), [12](decisions/12-encryption.md) |
| 38 | Running on plain Node | First build step for hosting. Instead of trusting the AI's count of 2 constructors Node couldn't run, turned on `erasableSyntaxOnly` and **let the typechecker list them: 4 files**, including `ProblemError` and a test helper the AI had missed. Rewrote them as ordinary fields. **I asked that local runs match Render as closely as possible,** so `npm run dev` and `npm run token` now use plain `node` too (`node --watch` for auto-restart), and `tsx` was removed. My laptop was on Node 24.4.1, older than 24.12, where running TypeScript directly became stable; I checked how `nvm` was set up and updated to 24.21.0. 218 green, no test changed; created a user in Swagger on a fresh database. (While checking the scripts, the AI started `dev.ts` in its VM by mistake; it failed at once and nothing was left running.) | [05](decisions/05-hosting.md#how-the-api-runs-on-render-2026-09-13) |
| 39 | Safe error logs | Planned 3 tests: a real database failure must log **no name, email, or date of birth**, while the log **still has** the method, path, and PostgreSQL's error code; and a non-database error's message must not be logged either. **While writing them, reading the code changed the test:** a create saves the user row, then phones, then addresses, and only the failed query is logged, so a trigger on addresses (as in the rollback tests) would only have exposed the street. The trigger went on the `users` row instead. One existing test had to change, since it required the full error to be logged. **3 failed, 60 passed, as predicted**; the red output showed the whole row in `params`: `Zelda, Quimby, zelda.quimby@example.com, 1971-03-14`. Fixed with an allowlist of safe fields (method, path, error name, code, constraint, the stack's `at` lines); no message is ever logged. **221 green.** | [11](decisions/11-database.md#found-along-the-way-personal-data-in-server-logs-open), [testing](testing.md#d-data-leaks-9-tests-leakstestts) |
| 40 | Swagger served by the API | Planned 8 tests about **what is public and what isn't**: the docs page, Swagger's files, and the spec need no token; the spec's server is `/`; path tricks out of the docs folder get nothing; `/v1/users` still needs a token. Installing `swagger-ui-dist` added a second package, **`@scarf/scarf`, which reports install statistics** on every `npm install`; I had it turned off. **Before running anything, the AI caught its own test gap:** supertest resolves `..` and `%2e%2e` before sending, so the path-trick tests would have asked for `/package.json` and proven nothing; they now send the path raw, as an attacker's client can. **4 failed, 67 passed, as predicted.** A check of the built routes (no database) then showed **Swagger's sample Petstore page was also being served**; switched to an allowlist of the 2 files the page needs, and added 2 tests for it (green only, since they came after the fix). Deleted the old docs page and `netlify.toml`; `website/` keeps a placeholder for the web client. **231 green**; Swagger works at `localhost:3000/docs` with no second server. | [05](decisions/05-hosting.md#swagger-served-by-the-api-netlify-kept-for-the-web-client-2026-09-13), [testing](testing.md#g-public-docs-10-tests-public-docstestts) |
| 41 | Settings checked at startup | Moved every environment variable into one checked place, so **a typo on Render stops the deploy with a clear message** instead of running quietly with a default (`DEMO_MODE=yes` would otherwise have meant "off"). Added a rule: `JWT_SECRET` must be at least 32 characters. I agreed to add `DEMO_MODE` and `MAX_USERS` now, though later steps use them. 14 tests, including one proving **the secret is never printed** in the error, and one listing several mistakes at once. The whole file failed to load before the code existed, as predicted; the other 115 bad-call tests passed. A check in the VM showed the real server refusing to start with 4 problems listed, before touching any database. **245 green**; `npm run dev` now prints proxy hops, demo mode, and user limit. | [05](decisions/05-hosting.md#settings-checked-at-startup-2026-09-13), [testing](testing.md#g-server-settings-14-tests-settingstestts) |
| 42 | User limit | Spec first: create user's `409` now has two examples, email taken and **user limit reached**. 4 tests with a limit of 3: the 4th user is refused even when one of the 3 is deleted; update, delete, and restore still work at the limit; the 3rd user fits exactly; and a race for the last spot. **The race needed a new trick:** the count happens inside the save, out of the racing repository's reach, so a temporary trigger pauses every insert for 0.3 s, long enough for both requests to count 2 without a lock. **Exactly the predicted 2 failed.** Built it with a PostgreSQL advisory lock around the count and the insert: 249 green. **Then I checked the race test by breaking the code on purpose:** commented out the lock, A7 failed (`201, 201`, 4 users); put it back, it passed. That's mutation testing done by hand, the idea from row 29. | [05](decisions/05-hosting.md#demo-data-50-starting-users-and-a-200-user-cap-2026-09-13), [testing](testing.md#a-two-requests-at-once-7-tests-racestestts) |
| 43 | Demo token | **I asked for the plan in simpler words,** and then recognized it: this is question 3's option A, a way for Swagger visitors to get a pass while still seeing auth refuse them without one. Spec first: a **Demo** tag at the top of the page with `POST /demo/token`, no lock icon, and a "how to try it" line in the intro (which also still said "in-memory storage", now fixed). 5 tests: get a 1-hour admin token; it works; **with a fake clock, it works at 59:59 and is refused at 1:00:01**; with demo mode off, nobody gets one; `GET` gets `405`. **4 failed, 74 passed, as predicted.** **I asked whether `Cache-Control: no-store` really protects anything,** since a browser can ignore it. It doesn't stop the visitor or a hostile browser, only well-behaved caches that would otherwise keep a copy for the next person; the 1-hour expiry and HTTPS are the real protection, and OAuth requires the header anyway. Built the route and moved token signing into one function shared with `npm run token`. **254 green.** | [05](decisions/05-hosting.md#decided-at-hosting-time-2026-09-13), [testing](testing.md#h-demo-token-5-tests-demo-tokentestts) |
| 44 | Starting users and nightly reset | Last build step before deploying. **Two corrections from the AI before writing code:** starting users need 1–3 phones and addresses, not 0–3 (the API's own rules require one), and the reset doesn't need "the same lock as the cap," because emptying a table already locks it. After my lock-removal check in row 42, it dropped a test that could never fail instead of keeping it. 9 tests: every starting user passes create's own validation; they're added only to an empty database; after a visitor adds, vandalizes, and deletes, the reset brings back exactly the 50; **a reset that fails midway, after the tables were emptied, leaves everything as it was**; and the time to the next 08:00 UTC. The file failed to load before the code existed, as predicted. Built the 50 users from hand-written lists (fake `@example.com` emails, phone numbers in the range reserved for fiction), the reset as one transaction, and `npm run dev:demo` to run demo mode locally. **263 green.** On a fresh local database: 45 users over 3 pages, the notice at the top of the docs, and a demo token that worked. | [05](decisions/05-hosting.md#keeping-the-demo-healthy-without-me-a-nightly-reset-2026-09-13), [testing](testing.md#e-the-demos-data-9-tests-demo-datatestts) |
| 45 | Live on Render | **I asked to go one tiny step at a time** through the dashboard. Kept my existing workspace, made a new project, and created the database first: PostgreSQL 18 in Ohio next to my games. **I questioned two defaults:** why not the $0 database (deleted after 30 days) and why not 5 or 10 GB of storage (1 GB is allowed, and storage can grow but never shrink). Deleted only the database's own "allow everywhere" rule, leaving the workspace rules that also cover my games, so all internet traffic to it is blocked. Render could only see my game repo on GitHub; added this one. I pasted the database URL, password included, into the chat; the AI flagged it, and since outside access is blocked I chose to keep going. **The first deploy worked:** Node 24.21.0 from `.nvmrc`, 0 vulnerabilities, 50 starting users, and in Swagger a `401` without a token, then 45 users with a demo token. **Measuring `TRUST_PROXY` found a real problem:** with `1`, 150 calls one at a time never got blocked. I noticed the calls speed up partway, and the AI's first command had printed nothing until the end, so the result was unclear. Sending 110 calls at once still never got blocked, so the API wasn't seeing my real IP: trusting 1 hop picked a Cloudflare address that changes per call. **Set `TRUST_PROXY=2`, no code change:** blocked after 100, a fake `X-Forwarded-For` still blocked, and my phone on cell data still got through. That's exactly why it was a setting instead of a guess. | [05](decisions/05-hosting.md#deployed-2026-09-13), [10](decisions/10-api-conventions.md) |

## 2026-09-13 — Admin web client

| # | Step | What happened | Record |
|---|---|---|---|
| 46 | Where the client lives | Before starting the client, **I asked for a "Start here" block near the top of the README**: busy visitors should find how to try the API in Swagger, and this journal, within seconds, not 60 lines down. **I switched the client's host from Netlify to Render.** I asked for pros and cons in simple terms: one dashboard and a Git deploy (which shows in the repo how it's deployed) against Netlify's zip upload, my habit. The AI flagged a catch: without **build filters**, a client-only commit would also redeploy the API. **I chose Render.** Next: 5 look-and-feel mockups before any code. | [05](decisions/05-hosting.md#web-client-on-render-not-netlify-2026-09-13) |
| 47 | Look and feel | **Asked for 5 mockups before any code**, static pages showing the same two screens so only the look differed: a clean internal tool, a developer console, an engineering notebook, a dense back-office grid, and a bold modern one. **Found while making them:** the list call returns only name and email, but the first drafts' tables showed city, phones, and version, which would cost one extra call per row; the AI removed them so every mockup shows only what the API really sends. **I picked the engineering notebook**, because non-technical visitors can follow its checks: each is written as an experiment with a claim, a prediction, and a stamped result, the same predict-then-run habit the tests were built with. **I asked that visitors can see the mockups, not just read about them:** full-page screenshots now sit in decision 13, and the pages deploy with the client at `/mockups/`. My first screenshot was a picture of my screen, cut off and zoomed, but it showed form boxes running past their columns in 4 of the 5 mockups, fixed before retaking. | [13](decisions/13-web-client.md) |
| 48 | Client stack | Compared plain HTML/JS, Vite with TypeScript, and Vite with React and TypeScript. **I know plain JS much better, but chose React**, since it's what employers look for; the AI recommended it too. Both TypeScript options share a key point: types generated from `openapi.yaml`, so a spec change the client doesn't follow stops it compiling. | [13](decisions/13-web-client.md#stack-vite--react--typescript-2026-09-13) |
| 49 | No scrolling, big screens only | Before building, **I asked whether the admin could avoid scrolling**: in the picked mockup the list was on top and the opened user below it. The tabs already kept the experiments on their own screen, so the real problem was the scroll inside the users screen. The AI listed 5 layouts (two-page spread, slide-over panel, pop-up, separate page, row opening in place) and recommended the spread, which suits the notebook look. **I decided phones don't matter** for an admin tool, so the spread needs no fallback, and asked that the project say so: decision 13 and the client's README now say big screens only. The AI drew it as mockup C2, next to C rather than replacing it, so the change stays visible. **My screenshot found a problem:** the users screen fit, but on the half-width pages the experiments' text wrapped until the second entry ran off the page. **I picked the AI's fix:** experiments now work like users, a list on the left page and the chosen one on the right, with the requests it sent and the API's answer. |
| 50 | Client project set up | The AI wrote the smallest Vite + React + TypeScript project by hand in `website/`, and one test: the app opens on "Users" with the demo notice. **The AI checked the versions again before writing** and found `@testing-library/react` needs `@testing-library/dom` installed next to it. **Predicted:** install clean, typecheck passes, test fails because the page is empty. **All three happened:** 106 packages, 0 vulnerabilities, and the failure showed an empty `<div />`. Then the heading and notice: test passed, build passed, and `npm run dev` showed the page. **Checking the mockups found a surprise:** `/mockups` and `/mockups/` both showed the app instead. With no file at that address, the dev server sends back the app's page, which is what a single-page app needs; only `/mockups/index.html` works, so the docs now use that. | [13](decisions/13-web-client.md#project-setup-2026-09-13) |
| 51 | Users list talks to the API | **I agreed to two picks:** the app gets a demo token automatically on open, and keeps it in memory only. Types for the client are now generated from `openapi.yaml` (`npm run api:types`). Tests first, with MSW as a fake API inside the tests, and non-happy cases first: API unreachable, no demo token (`401`), list refused (`500`), then the normal case (token sent, first page sorted by last name). **4 failed as predicted.** Then the list code, predicted all green, **but 4 still failed, and the page dumps showed the real starting users from Render.** The tests had reached the live API: `openapi-fetch` keeps the `fetch` that exists when the file loads, before MSW swaps in its fake one, so MSW never saw the requests and its "fail on any unexpected request" setting never fired. **Two fixes:** the client looks up `fetch` on each call, and the tests use `http://fake-api.test`, an address that can't exist. **5 green.** **Then I asked to check the second fix by breaking the first on purpose:** 2 passed, 3 failed, no real users anywhere, exactly as predicted, including the "unreachable" test passing for the wrong reason. Fix put back. | [13](decisions/13-web-client.md#project-setup-2026-09-13) |
| 52 | First look in the browser | **I asked to see it in the browser before any styling.** The AI predicted a page saying "Can't reach the API" even though the API was fine: the browser blocks answers for pages whose address isn't in `CORS_ORIGINS`, and to our code a blocked answer looks like no answer at all. The tests missed this because Node doesn't enforce CORS; only browsers do. **Exactly that:** the page said "Can't reach the API", and the console said "blocked by CORS policy". Each request showed up twice, React's dev-mode double run. On Render, one small step at a time: the variable wasn't there; added `CORS_ORIGINS=http://localhost:5173`; **Save and deploy** (same build, new setting) rather than a rebuild. I mentioned I'd **turned off auto-deploy**, since I push often as a backup; the docs said "auto-deploy on commit" and are fixed. The startup log printed `CORS allowed origins: http://localhost:5173` (the AI had said it wouldn't). **Reloaded: the real 45 users, page 1 of 5, as plain unstyled text.** | [05](decisions/05-hosting.md#deployed-2026-09-13), [13](decisions/13-web-client.md) |
| 53 | The notebook look | Moved mockup C2's look into the app: the open spread, grid paper, red margins, ink-blue data. **Fonts installed as packages** (Fontsource) instead of the mockups' Google Fonts link, so versions are pinned and visitors' browsers make no call to Google; I agreed. **Only working controls are shown:** search, paging buttons, tabs, and New user appear when they work, so nothing on screen is fake. Predicted the 5 tests stay green, since they find things by role and text, not by looks: **5 green**, and in the browser the real users sit on the left page with a note on the blank right page. | [13](decisions/13-web-client.md#project-setup-2026-09-13) | [13](decisions/13-web-client.md#layout-the-notebook-lies-open-2026-09-13) |
| 54 | Open a user | **I picked opening a user over paging first.** Clicking a row fills the right page with the full user, as text on the notebook's lines, since editing isn't built and dead input boxes would mislead. **Before the tests, I stopped to ask why the list was showing at all:** the app quietly gets a demo token on open, as agreed, but nothing on screen says so; I asked to work on that right after this step. 6 tests, non-happy first: user deleted meanwhile (`404`), API unreachable, **two quick clicks where the first answer arrives last** (the client's version of the API's race tests), never a blank page while loading, the normal case (token, `view=detailed`, highlighted row), and opening from the keyboard. **Predicted quick failures, since no clickable last name existed: 6 failed in under 3 s.** Built a shared in-memory token, one place that turns calls into data or a readable message, and a right page where each answer remembers which user it belongs to. **11 green.** In the browser I clicked through several real users. | [13](decisions/13-web-client.md) |
| 55 | The demo token on screen | Picked from the AI's options: when there's no token, **a notebook page explaining why** replaces the list; the token comes from a **Get demo token button** (I reversed the automatic pick, so visitors see the step); and **expiry is shown**, not renewed quietly. I approved the wording of all 5 states (none, getting, couldn't get one, active with a corner countdown, expired or refused) before any test. 7 tests, non-happy first: nothing is sent before the click; unreachable, then Try again works; `429` counts down from the API's `Retry-After` with the button paused; `401` in the API's words; **the hour runs out** (list and opened user replaced, nothing sent with the dead token, a new token brings the list back); `401` during use; and the corner going from 60:00 to 59:59. Time is faked, so an hour passes in milliseconds. **1 passed and 15 failed, as predicted.** After the code, the AI warned the 3 fake-clock tests were the riskiest; instead **those 3 passed and the other 11 failed**, all on one line of the AI's test helper (passing `advanceTimers: undefined` replaces user-event's default and breaks every click). Fixed: **16 green**; in the browser, the no-token page, then the countdown. **The AI pointed out one guard had no test:** a late `401` for an old token must not end a new one. I asked for the test and the check by breaking: it found **two** layers (the guard, and the right page ignoring answers after it's removed). With nothing removed, 17 green; guard removed, still 17 (the second layer caught it); both removed, **the new test failed**; both put back, 17 green. | [13](decisions/13-web-client.md#the-demo-token-on-screen-2026-09-13) |
